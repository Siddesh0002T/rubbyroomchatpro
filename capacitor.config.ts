import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rubbysoft.rubbyroomchatpro',
  appName: 'Rubby Room Chat Plus',
  webDir: 'dist'
};

export default config;
